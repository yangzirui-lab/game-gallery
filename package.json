{
  "name": "game-gallery",
  "version": "1.0.0",
  "private": true,
  "description": "游戏收藏管理平台 - Monorepo",
  "workspaces": [
    "apps/*",
    "games/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/web",
    "dev:fund": "npm run dev -w apps/fund",
    "build": "npm run build -w apps/web",
    "build:fund": "npm run build -w apps/fund",
    "build:all": "npm run build -w apps/web && npm run build -w apps/fund",
    "prepare": "husky || true"
  },
  "lint-staged": {
    "apps/web/**/*.{js,jsx,ts,tsx}": [
      "bash -c 'cd apps/web && npx tsc --noEmit -p tsconfig.app.json'",
      "eslint --config apps/web/eslint.config.js --fix",
      "prettier --write"
    ],
    "apps/fund/**/*.{js,jsx,ts,tsx}": [
      "bash -c 'cd apps/fund && npx tsc --noEmit -p tsconfig.app.json'",
      "eslint --config apps/fund/eslint.config.js --fix",
      "prettier --write"
    ],
    "*.{json,css,scss,md}": [
      "prettier --write"
    ]
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/catalyzer-dot/game-gallery.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/catalyzer-dot/game-gallery/issues"
  },
  "homepage": "https://github.com/catalyzer-dot/game-gallery#readme",
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.8.0"
  }
}
